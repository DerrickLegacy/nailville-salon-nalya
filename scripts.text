

ALTER TABLE users 
ADD COLUMN admin BOOLEAN DEFAULT 0;


ALTER TABLE `users`
MODIFY `admin` TINYINT(1) NOT NULL DEFAULT 0 COMMENT '0 = user, 1 = admin' AFTER   remember_token ;

ALTER TABLE `users`
ADD COLUMN `activity` ENUM('Active','Inactive') NOT NULL DEFAULT 'Active' COMMENT 'User status' AFTER `admin`;
